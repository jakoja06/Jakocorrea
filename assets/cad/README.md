# CAD Models

Place your 3D model files here (.step, .iges, .stl, .zip).

To add a model to a project, update the `assets` object in `data.js`:

```javascript
assets: {
  cad: [
    { title: "Final Assembly", url: "./assets/cad/assembly.step", type: "STEP", size: "45 MB" }
  ]
}
```
